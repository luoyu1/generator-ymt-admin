'use strict';
/**
 */
(function() {
    myApp.controller('homeController', ['$scope',
        '$rootScope',
        '$state',
        '$stateParams',
        'backendSession',
        function($scope,
            $rootScope,
            $state,
            $stateParams,
            backendSession) {
        }
    ])
})();
