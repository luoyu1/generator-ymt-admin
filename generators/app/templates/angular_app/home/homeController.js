'use strict';
/**
 */
(function() {
    myApp.controller('homeController', ['$scope',
        '$rootScope',
        '$state',
        '$stateParams',
        function($scope,
            $rootScope,
            $state,
            $stateParams) {
        }
    ])
})();
